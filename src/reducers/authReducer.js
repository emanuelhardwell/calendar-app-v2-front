const initialState = {
  checking: true,
  // uid: "1233234245252",
  // name: "Emanuel"
};

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
    // case value:
    //   break;

    default:
      return state;
  }
};
